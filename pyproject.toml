[project]
name = "hackupc-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "pydantic-settings>=2.14.0",
    "pymongo>=4.17.0",
]

[tool.uv.workspace]
members = [
    "backend",
]
